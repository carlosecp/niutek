// & NOTA
// Luego cambiar las props de la data a un tipo probablemente que se encargue de definir el tipo de valor esperado por cada una de las rows de la tabla.
interface Props {
	data: any[]
}

const Table = ({ data }: Props) => {
	return <div></div>
}

export default Table
